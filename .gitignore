# Secrets
.env

# Python
.venv/
__pycache__/
*.pyc

# Local demo state (created by stage0/app.py and stage0/demo.py)
stage0/memory.db*

# Local browser smoke-test snapshots
.playwright-cli/

# Stage 7 backup/export artifacts (stage0/backup.py)
stage0/backups/
stage0/exports/

# Frontend build artifacts (frontend round, 2026-09-06)
frontend/node_modules/
frontend/dist/

# Browser-automation deps installed at the repo ROOT by the acceptance rounds
# (playwright + playwright-core, ~19MB).  Only frontend/node_modules was
# ignored, so a root-level `git add -A` would have committed them.
node_modules/

# IDE
.idea/

# Local run artifacts, logs and eval snapshots (not for upload)
.artifacts/
output/

# Frontend incremental build info
*.tsbuildinfo

# Non-project working files (course doc, agent-prompt working notes) — local only
/24050532-*.docx
/Agent Harness分级改造Prompt.md
/docs/agent-product-upgrade-prompts-2026-09-07.md
/docs/harness-upgrade-prompts/
