# 三分钟演示脚本：从事实到可审计预警

React 完整演示：已有药单 → 导入 CSV/图片用药表 → 原件与差异对照 → 确认并查看后台检查 → 留下缺失信息待办、离开后补充恢复 → 下载就诊摘要。使用独立临时合成库，启动、样例与浏览器验收步骤见 [P0–P6 交付说明](docs/product-upgrade/final-acceptance/README.md)。证据原文与变更影响的旧流程仍可用；下文保留原 Streamlit 演示，涉及清空数据库的旧步骤仅适用于明确的演示库，不要用于正式记录。

这是一个按屏幕点击的短流程。它使用真实 `MedicationCoordinatorAgent`、`MemoryStore` 和 DDI detector；默认不需要 LLM 或网络。

1. **启动并清场（0:00–0:15）**
   - 在仓库根目录运行 `streamlit run stage0/app.py`。
   - 左侧勾选“我确认清空 stage0/memory.db”，点击“清空并重新开始”。
   - 看到顶部显示“确定性离线路径”、当前用药 `0`。

2. **登记患者档案（0:15–0:40）**
   - 在“患者档案”填写：年龄 `72`、性别 `女`、肾功能 `轻度受损`。
   - 过敏史填 `磺胺`；慢性病填 `高血压、糖尿病`；偏好填 `care_notes=希望用大字版清单`。
   - 点击“保存患者档案”。右侧应出现已保存事实与 `memory:semantic:...` 引用。

3. **建立基线用药（0:40–1:05）**
   - 进入“用药变更 → 新增药物”，依次新增 `氨氯地平`、`二甲双胍`、`阿司匹林`，每次点击“新增并主动检查”。
   - 每次新增都写入右下“记忆账本”的当前用药与变更时间线。

4. **触发克拉霉素主动预警（1:05–1:30）**
   - 新增 `克拉霉素`。
   - 右侧“本次协管结果”应出现 `氨氯地平 × 克拉霉素` 的主动警告，包含 severity、confidence、来源 URL/中文摘录，以及 `memory:episodic:...` → `memory:conclusion:...` 审计链。
   - 展开“查看来源与审计链”，强调代理只提示风险，没有给出停药或改量指令。

5. **展示肾脏谨慎与低置信度升级（1:30–1:55）**
   - 新增 `布洛芬`。
   - 观察肾功能/年龄相关说明书慎用提示；同时观察 `阿司匹林 × 布洛芬` 的类别推断被标为 `low`，代理循环里会出现额外 reflection 检索，并升级“建议咨询医生/药师”。这条是保守类别匹配，不是说明书直接点名的药物对。

6. **报告 CT 造影并保留冲突（1:55–2:20）**
   - 打开“CT 造影”，保留默认文本“医生上个月让做的 CT 用了造影剂”。
   - 点击“记录暴露并检查冲突”。
   - 在“风险预警”和“记忆账本”中展开未决冲突：照护者报告的医疗行为与二甲双胍/含碘造影剂标签风险两侧都被保留，系统不替医生判定对错。

7. **开启第二会话并精确回忆（2:20–3:00）**
   - 左侧点击“开启新会话（保留记忆）”；顶部会话编号递增，数据库不清空。
   - 进入“记忆查询”，输入 `现在吃什么药`，点击“从 memory.db 精确查询”。
   - 右侧返回当前在用药清单和跨会话变更时间线，行尾带 `memory:medication:...` / `memory:episodic:...`。这证明回答来自重新打开的 SQLite 记忆，而不是本次 Python 进程中的聊天上下文。

演示结束后可展开任一警告的审计链，或再次点击“清空并重新开始”回到干净状态。
