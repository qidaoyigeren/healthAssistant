# 最终交付：本地受控试用版本

> **这是一个本地、受控的试用版本。** 它不是一个医疗器械，**不提供任何用药安全保证**，
> 也**没有连接**任何专业医护服务（复核流程用的是本地模拟工作台）。所有结论都带来源，
> 但来源不等于正确；用药调整必须由本人或专业人员确认。
>
> **它已经验证到什么程度：** 工程机制（记录版本化、必要检查、事项与回访、候选确认、
> 跟进安排、重启恢复）有离线与页面的逐步验收；**开放式的真实模型自主能力尚未通过完整
> 验收**——脚本化规划器跑通**不等于**真实 Agent 自主成功（见 §6）。

---

## 1. 已交付的用户能力

**记录真实发生的用药变化，并让它接上长期跟进。**

在回访页（`/safety/:caseId/visit`）的「**补充情况**」里用自己的一句话说明即可：

| 用户说 | 系统做什么 |
|---|---|
| "合成药甲改成每天两次了" | 一条**调整用法**候选（中文数字照收） |
| "上周已经停了合成药甲" | 一条**停用**候选；"上周"保留原话，不解析成某一天 |
| "开始吃合成药丙了，2mg" | 一条**新增**候选 |
| "合成药甲又接着吃了" | 一条**恢复服用**候选，指向那条具体的停用记录 |
| "之前登记停用是填错了" | 一条**纠正记录**候选，回到原来那个服用阶段 |
| "合成药甲换成合成药丙了" | **一组**两条候选（换出 / 换入），可整组一次性确认 |
| "打算下周停药甲" | **不进**可确认列表，列为"还没发生的事" |
| "药甲要不要停" / "昨天漏了一次" | 不生成候选 / 记成用户报告，**都不改药单** |
| "这个药改了"（说不清哪个） | **补问**"您说的是哪一种药？"，不猜 |

**确认之前，当前药单一个字节都不动。** 确认之后：走既有受控入口写入 → 必要安全检查
按药单版本重新排队 → 相关事项更新 → **同一次回访继续**。

配套的还有：用药记录详情里的**服用阶段**与**变更记录**（发生时间与登记时间分开、
时间来源如实标出）；安全事项的**跟进安排**（安排 / 确认 / 改期 / 取消，到期由 worker
自动触发）；以及原有的必要检查、材料核对、待办与时间线。

**模型是可选配置。** 不配凭据也能完整使用——只有「补充情况」的自然语言理解需要它，
没配时界面如实说"尚未完成自动理解"、原文保留，改用结构化登记入口即可。

---

## 2. 快速启动步骤

**唯一推荐运行目录**：`D:\py\HealthAssistant.worktrees\integration`（分支
`feat/medication-change`）。主工作区 `D:\py\HealthAssistant` 停在更早的提交上，
**没有本轮的功能**——在旧目录里启动会看不到「补充情况」。

```powershell
cd D:\py\HealthAssistant.worktrees\integration
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r stage0/requirements-stage1.txt

# 终端 1：后端（单写进程；worker 随它一起起，不需要另开进程）
python -m uvicorn stage0.server:app --host 127.0.0.1 --port 8000

# 终端 2：前端
cd frontend; npm install; npm run dev
```

打开前端提示的地址（默认 `http://localhost:5173`）。

**确认 worker 在跑**：`http://127.0.0.1:8000/v1/health` → `"worker_thread": true`。
`pending_outbox_tasks` 长期不降说明它没在推进。没有 worker 时页面仍显示已保存的一切，
但必要检查、回访推进、跟进到期都不会发生。

**数据库**：`stage0/memory.db`（`STAGE0_DB_PATH` 可改）。**产物**：`stage0/backups`、
`stage0/exports`。数据库、备份、导出、`frontend/dist`、`output/` 都在 `.gitignore` 里。

**停止**：两个终端各自 `Ctrl+C`；worker 是后端进程内的守护线程，随之退出。

> 上面这条后端命令**本轮实际跑过一次**（指向一个隔离库，不碰默认库）：
> `/v1/health` 返回 `status: ok`、`worker_thread: true`、`schema_version: 4-p2`。
> 前端 `npm run dev` 与 `npm run build` 也在本轮验收里跑过。

**模型（可选）**：在 `stage0/.env`（已被 gitignore）里配，并**显式**选端点：

```
LLM_PROVIDER=siliconflow
SILICONFLOW_API_KEY=…
```

`LLM_PROVIDER` 是显式选择器；不设时按 `zhipu → tokendance → openai_compatible →
deepseek_legacy` 顺序探测第一个有 key 的。**换端点是一个决定，不应该是"某个 key 恰好
在本机"的副产品。**

---

## 3. 核心流程验收结果及对应版本

| 验收 | 对应源码 | 结果 |
|---|---|---|
| 离线闸门 `output/gate-v3` | `64252c6` | **`status: pass`，`source_unchanged: true`，57 套件 / 1097 测试全过**，评测项无失败，主线六类全过 |
| 最终用户流程（实际页面 + 真实后端）`output/final-flow-3` | `64252c6` | **51/51 步通过** |
| 用药变更离线用例（`stage0/test_medication_change_flow.py`） | `64252c6` | **40 个用例通过** |
| 备份 / 校验 / 恢复（隔离库）`output/backup-check` | `64252c6` | **通过**（见 §4） |
| 有限真实模型验收 `output/medchange-live-2`、`-3` | `15992a4` 之前 | **仅"理解"一项通过**（见 §6） |

**这些结果都对应当前源码**：闸门在跑之前与跑之后各算一次源码指纹，两次相同
（`source_unchanged: true`）才判通过。更早的 `output/gate-v2` 也是绿的，但它对应的
源码与现在的不同（本轮又改了验收夹具），所以**没有**拿它拼进这一次通过。

### 最终用户流程验收覆盖的十步

1 打开当前用药记录与安全事项 ｜ 2 开始 / 继续**同一次**回访 ｜ 3 提交补充信息 ｜
4 查看待确认候选及其**来源** ｜ 5 **确认前药单不变** ｜ 6 确认后记录、历史与必要检查
正确更新 ｜ 7 相关事项继续、**未决风险没有被自动清除** ｜ 8 保存并确认跟进安排 ｜
9 **修改 / 取消 / 到期触发都有真实效果**（到期由 worker 扫描触发，不靠用户再点一次）｜
10 **重启服务后**，记录、原文、候选与待办仍可恢复。

自然语言那一部分用**明确标注的脚本化解释器**验证工程流程——它**不计为**真实模型能力。

### 闸门

```
python scripts/verify-agent-closeout.py --out output/<新的目录>
```

输出按类别分开，不合并成一个通过率；目录不可复用；`source_unchanged` 比对运行前后
的源码指纹，运行中被改动即判 fail。

**本轮它真的拦下过两次**——都是我在闸门运行期间继续改源码，于是被判
`source_unchanged: false`。那两次失败**不是**产品的问题，是工具在正确地拒绝签字：
不改完就不知道签的是哪一版。第三次在冻结的源码上跑，才拿到 `pass`。

---

## 4. 数据备份与恢复方式

用 **SQLite 的 backup API**（运行中也安全）。**不要**直接复制 `.db` 文件——WAL 模式下
那样可能拿到一个缺内容的副本。

```powershell
# 备份（产物含 .sha256 与 .counts.json 边车文件）
python -m stage0.backup --db stage0/memory.db --backup --out stage0/backups

# 校验：完整性、快速检查、外键、边车校验和
python -m stage0.backup --verify stage0/backups/memory-<时间戳>.db

# 恢复到**新路径**（拒绝覆盖已存在的文件）
python -m stage0.backup --restore stage0/backups/memory-<时间戳>.db --to stage0/restored.db
```

恢复拒绝覆盖已存在的目标是**有意的**：恢复不该悄悄盖掉一份还活着的库。要换到线上，
请先停服、把旧库改名留档，再把恢复出来的文件指到 `STAGE0_DB_PATH`。

**已在隔离库上验证过一次**（`output/backup-check`）：备份 → `sha256` 匹配、`integrity_check`
与 `quick_check` 均 `ok`、外键违规 0 → 恢复到新路径后行数一致、用药记录读得出 →
再次恢复到同一路径被正确拒绝。

---

## 5. 已知限制

**已记录、本轮未解决（非阻塞）**

1. **部分自然语言表达尚未验证**：真实模型只在一小段场景上被观察过；"计划 / 已发生"
   的区分、跨句指代等没有跑通完整验证。
2. **跨多次提交的换药分组有限**：同一句话说清的换药是一组，可整组原子确认；分两句说
   则各成一组，只能分别确认（同一味药同一件事的候选会合并对齐）。
3. **部分纠错操作尚不支持**：只覆盖"误登记停用"的纠正；"之前登记的剂量填错了"走普通
   调整，"之前登记的恢复记录是错的"会被判为与真实记录冲突而拒绝。
4. **模糊时间保留原文**："上周""这两天"不会被解析成具体日期，也不会结构化成一个区间。
5. **真实模型自主回访尚未通过完整验收**（见 §6）。

**沿用既往轮次、未因本轮改变**

- KEGG 是佐证而非权威中文说明书依据；`P` 级相互作用在缺少更强证据时默认 `moderate`；
  类别推断是低置信度的、会强制升级而非直接断言。
- 材料核对的交付在覆盖不全时恒为 `partial`，真实模型也**从不主动外部取证**。
- **"不得凭模型自行生成临床阈值或检查周期"只做到结构性约束**，没有对**问题措辞**的
  机械检查——模型没有权限把阈值写进记录，但它仍可能在提问里提到一个阈值。

**设计上的取舍（不是缺陷）**

- 停药之后"触发条件不再出现"与"整体风险已解除"**分开表达**；后者系统从不主张
  （`closure_evidence.nature.overall_risk_resolved` 恒为 `false`）。是否允许关闭沿用
  既有依据规则，本轮没有改。
- 未确认的用户口述**不会跨事项共享**——把一句没核对过的话在两件事项上同时生效，比
  多问一次更糟。

---

## 6. 真实 Agent 能力的实际验证情况

**这一节请按字面读：能力边界比功能列表窄。**

| 环节 | 状态 |
|---|---|
| **自然语言理解**（一句话里的对象 / 字段 / 时间 / 不确定） | **通过**（有限场景） |
| 候选确认与原子写入 | **未在真实模型上走到** |
| 用药阶段与纠错历史 | **未在真实模型上走到** |
| 安全事项与回访承接 | **未在真实模型上走到** |
| **开放式调查中的自主规划 / 自主回访** | **未通过**（沿用既往轮次结论） |

**已观察到的**：给出"这个药不吃了，换成另一种了"，真实模型读出 `object_ambiguous`、
自己提问"您说的是哪一种药？"、**没有编一个药名**——这正是要的行为（1 次调用，
1688 tokens 实测）。

**没有走到的**：第二句里模型**自相矛盾**（点名"合成药甲"却又标了"对象不明确"），
我的代码当时照抄了这个标记，候选被挡下，后面三项都没走到。**这条已经修掉**
（歧义改由"能不能唯一解析到一条权威记录"判定），并有离线回归用例；但**没有为了
追一个通过再花一次真实调用**。所以那三项的真实模型表现**至今未知**。

**成本口径**：该轮声明上限 4 次调用，**实际用掉 5 次**（1 次探针 + 2 次被我自己脚本
缺陷中断的批次 + 2 次有效批次）。超出部分来自我的工具缺陷与场景措辞返工，不是模型
重试。本轮**没有调用真实模型**，此前结果原样保留。

**不要把这些读成**：整体用药安全有保证、模型可以自主判断风险、系统已经接入专业医护。
都不是。**脚本化规划器通过 = 机制通过，不等于真实 Agent 自主成功。**

---

## 7. 最终版本与问题反馈

- **交付位置**：`D:\py\HealthAssistant.worktrees\integration`
- **分支**：`feat/medication-change`
- **源码版本（闸门与流程验收校验过的）**：`64252c6`
- **本文件的提交**：见该分支最后一次提交（`FINAL-DELIVERY.md`）
- **未合并到 `master`**：主工作区保持原样，没有覆盖、没有重置分支

**遇到问题时最小可复现信息**：在哪个页面、点了什么、页面原话（服务端消息会原样显示）、
`http://127.0.0.1:8000/v1/health` 的输出、以及当时 `stage0/memory.db` 的**备份**
（用 §4 的方式做，不要直接复制）。

**反馈表模板**：[docs/review-visit/TRIAL-FEEDBACK.md](docs/review-visit/TRIAL-FEEDBACK.md)
——关注三件事：是否减少重复录入、是否看懂下一步、是否完成跟进。模板只供自己填写，
不联系任何人、不收集真实健康数据。

**更细的记录**：[MEDICATION-CHANGE-REPORT.md](docs/review-visit/MEDICATION-CHANGE-REPORT.md)
（本轮）与 [docs/review-visit/REPORT.md](docs/review-visit/REPORT.md)（上一轮）、
[README.md](README.md)。
