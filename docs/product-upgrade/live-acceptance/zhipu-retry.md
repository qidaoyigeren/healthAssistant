# 智谱官方模型切换与复测（2026-09-08 至 09-09）

已配置并实际调用智谱官方 `glm-4.7-flash`，严格问答验收仍未通过：供应商返回 HTTP 429、错误码 1305，提示“该模型当前访问量过大，请您稍后再试”。密钥已成功完成模型请求，问题不是凭据无效。

## 配置

- `ZHIPU_API_KEY` 保存在被 Git 忽略的 `stage0/.env`，不写入报告。
- 官方接口：`https://open.bigmodel.cn/api/paas/v4/`。
- `ZHIPU_MODEL=glm-4.7-flash`、`ZHIPU_THINKING=disabled`、`ZHIPU_MAX_TOKENS=4096`。
- 新增智谱配置优先级，高于已有 TokenDance；保留旧配置作为兼容分支。
- 测试使用独立数据库、合成患者材料、真实本地检索与 KEGG；单次 Agent 预算仍为 180 秒，SDK 自动重试为 0。

## 结果

| 项目 | 结果 |
| --- | --- |
| 后端回归 | 332 项全部通过，包括新增供应商优先级及关闭思考回归 |
| 冷缓存相互作用抽取 | 通过；68.9 秒，真实抽取、KEGG HTTP、来源支持的警告及完整账本检查均通过 |
| 新增氨氯地平 | 6.2 秒，业务事件完成，但回答来源为模板兜底 |
| 新增克拉霉素 | 74.5 秒，业务事件完成，但回答来源为模板兜底 |
| 完整证据问答 | 6.1 秒，规划及回答请求遇到 429，最终模板兜底且无引用，严格失败 |

时间是本轮观测值，不是模型速度基准。尤其问答的 6.1 秒是快速失败，并非有效回答提速。服务当前的 `run_status=succeeded` 可以表示业务流程完成；本次验收另行检查 `response_source=llm` 和引用，未将该状态算作模型成功。

合计 20 次模型请求尝试：4 次取得实际响应，16 次失败估算；已知实际 token 共 7,566。免费档在这批真实调用中受到供应商容量限制，不能承诺稳定全链路可用。本轮未重复浏览器业务验收，未充值或自动切换收费模型。

## 证据

- [结构化结果](../../../output/product-live-zhipu-20260908/zhipu-summary.json)
- [完整问答响应](../../../output/product-live-zhipu-20260908/live-evidence-question.json)
- [检查点及供应商错误](../../../output/product-live-zhipu-20260908/checkpoint-diagnostics.json)
- [冷缓存结果](../../../output/product-live-zhipu-20260908/cold/result.json)
- [332 项回归日志](../../../output/zhipu-full-regressions.log)

后续重新启动应用即可加载本地智谱配置。供应商容量恢复后应使用新的测试事件身份再验收，避免读取旧幂等结果；不能通过循环发送请求或延长等待来将本轮失败标为成功。
