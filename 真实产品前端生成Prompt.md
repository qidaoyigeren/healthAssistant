# 用药协管员：真实产品前端生成 Prompt

使用方式：将下方「Prompt 正文」完整交给能够读取和修改本项目仓库的前端生成 Agent。本文依据 2026-09-05 工作区代码编写；执行时必须再次核对实际代码。本文是生成与实施指令，本轮尚未实现其中的新页面或新增接口。

## Prompt 正文

你是一名负责产品体验、前端工程和接口联调的资深工程师。请在当前 HealthAssistant 仓库中，为「用药协管员」实现一个完整、可运行、连接真实后端、数据持久化的中文 Web 应用。

直接完成实现与联调，不要只给设计方案、截图、页面骨架或静态组件。最终交付必须能让照护者完成「登记患者情况 → 记录实际用药变化 → 查看检查结果及来源 → 核实矛盾信息 → 跨会话查询和追溯」的完整流程。

本次范围是单照护者、单患者的本地或受控自托管应用。完整性以本项目业务闭环为准。真实产品体验不等于宣称系统已经具备医疗产品上市条件，也不等于添加尚不存在的医院接入、多租户、处方、支付、消息推送或可穿戴设备数据。

所有业务记录必须来自用户真实提交、现有数据库或后端真实计算。空数据库就展示空状态。不得使用 mock 数据支撑正式页面。

### 一、先读取仓库，建立真实能力清单

先遵循适用的 AGENTS.md，并阅读：

- `README.md`、`CONTEXT.md`。
- `stage0/server.py`：实际 FastAPI 服务与错误、异步事件契约。
- `stage0/api_client.py`：已有客户端调用方式，仅作为参考。
- `stage0/app.py`：原 Streamlit 页面与真实事件 payload，保留其运行方式。
- `stage0/agent.py`：CareEvent、AgentResponse、事件处理、工具执行和安全边界。
- `stage0/memory.py`：事实、药单、事件、冲突、双时间查询、版本、复查和审计。
- `stage0/memory_search.py`、`stage0/memory_context.py`。
- `stage0/ddi_engine.py`、`stage0/rag.py`：检测结果、证据结构与运行配置。
- `stage0/backup.py`：真实备份、导出、校验和恢复能力。
- `stage0/test_stage10_server.py` 及相关 memory 测试。
- `docs/production-upgrade-2026-09-05/stage8-10_implementation_report.md`。

特别注意：根目录 `main.py` 只有 Hello World 示例。业务服务入口是 `stage0.server:app`，不要接到示例服务上。

先输出一份简短的「页面 → 用户操作 → 真实接口 → 当前缺口」清单，然后立即进入实现。当前已有未提交修改时，在其基础上增量开发，不覆盖、不回退，也不要重置数据库或重建现有数据工件。

不得把设计文档中的规划项视为已完成代码。遇到接口不足，补齐本次闭环必需的最小后端适配，继续复用现有 Agent 和 MemoryStore。

### 二、不可违反的数据与产品约束

1. 正式运行路径不能引用 demo fixture、测试 golden cases、预制患者、硬编码药单、硬编码风险、随机统计值或伪造对话。测试目录可以使用明确标注的合成输入，但不能注入正式数据库或生产 bundle。
2. 不允许请求失败后返回本地假数组、伪成功 toast，或用 `setTimeout` 模拟服务处理成功。轮询、重试退避、输入防抖可以使用定时器。
3. SQLite 和后端业务逻辑是记录的权威来源。localStorage 仅用于字号、布局、筛选偏好等；不要用它保存一套独立患者数据库。
4. 新提交的记录必须在服务端确认后出现于正式药单、档案和历史记录。处理中可以显示单独的待处理条目，不能提前当作已生效事实。
5. 用户清空输入框不等于撤回已有事实，未填写不等于「没有过敏」「肝肾功能正常」；这些区别必须体现在表单和 API 操作中。
6. 保留后端给出的严重度、置信度、来源、记录版本和结论状态。不要在前端从一段说明文字反推风险等级或补造来源。
7. 不把当前数据当成历史快照，不把历史结论当成当前有效预警，不把过去成功的检查当成本次检查已完成。
8. 默认本地确定性处理、已持久化证据和真实 SQLite 写入都属于真实业务执行。是否调用大模型、是否访问外部数据源，是另外的运行配置，不能把「真实数据」偷换成「强制调用大模型」。
9. 正常运行不得读取 `.env` 内容到浏览器、输出密钥或上传患者信息给前端自行配置的第三方服务。现有 LLM 开关保持服务端配置，未明确开启时不自动开启。
10. 用户可读页面使用「患者档案、用药记录、待核实、来源、处理记录」等业务语言。SQLite、RAG、outbox、Agent trace、token budget 等技术信息放入诊断或审计详情。
11. 所有按钮必须有真实行为。缺少实现的非核心功能不放在主导航里占位；核心功能缺接口就实现适配，不能以 disabled 按钮代替完成。

### 三、技术实现要求

若仓库尚无独立 Web 前端，在 `frontend/` 新建应用，默认采用：

- React + TypeScript + Vite。
- React Router 管理独立页面、深链接和详情路由。
- TanStack Query 管理服务端查询、轮询、缓存失效和刷新。
- React Hook Form + Zod 管理表单和输入校验。
- Tailwind CSS 或等价 CSS token 方案；用 Radix 等可访问性基础组件实现弹窗、抽屉、菜单。
- Lucide 或同一套 SVG 图标，避免混用表情图标。

采用实际安装时相互兼容的版本并提交锁文件，不盲目指定未核实版本。已有可用技术栈时优先复用，不为形式重写。

建议目录：

```text
frontend/
  src/
    app/                  # 路由、provider、全局错误边界
    api/                  # HTTP 客户端、真实 DTO、校验、适配
    features/
      overview/
      profile/
      medications/
      alerts/
      conflicts/
      history/
      assistant/
      settings/
    components/           # 业务共享组件与基础 UI
    hooks/
    styles/
    utils/
  tests/
  .env.example
  README.md
docs/frontend/
  api-contract.md
  acceptance-report.md
```

要求：

- 页面不能到处散落 `fetch`；统一处理状态码、JSON 解析、取消请求、错误信息和请求追踪。
- DTO 与页面 ViewModel 分离。API 缺字段时显示未知或补齐后端，不用 TypeScript 类型断言假装字段存在。
- 前端 `/v1` 默认通过 Vite 开发代理连接 `http://127.0.0.1:8000`。生产部署通过同源反向代理转发 `/v1`，并正确处理 SPA 深链接刷新。
- 需要跨源访问时才配置明确 origin 的 CORS，不默认添加开放的 `*` 配置。
- 不重新实现 DDI、药名归一化、事实冲突策略、结论失效、引用验证或安全判定。
- 保留后端单写者进程约束，启动命令不能使用多个 uvicorn worker。新增长任务不得与现有 worker 并发共享同一 Agent 执行状态；复用受控队列或串行执行入口。
- 前端不直接读取服务端数据库文件。浏览器只通过 API 访问患者数据。

### 四、页面结构和视觉方向

产品名称：用药协管员。

核心用户：帮助父母整理长期用药记录的成年照护者。主要任务是迅速看清「目前记录了什么」「哪些信息需要核实」「依据来自哪里」。

视觉方向采用克制的「照护记录册」：保留现有项目的青绿、深蓝灰和浅灰白气质，通过清晰的记录行、时间标识和证据联动建立识别度。

基础 token：

| Token | 色值 | 用途 |
| --- | --- | --- |
| canvas | `#F3F7F7` | 页面背景 |
| surface | `#FFFFFF` | 内容表面 |
| ink | `#17324D` | 主要文字 |
| primary | `#2A7A78` | 主要动作、当前选中项 |
| caution | `#8B5D11` | 待核实与不确定性 |
| danger | `#B94846` | 高等级风险提示 |

中性色、边框和浅底色从上述 palette 形成统一 token，逐项验证对比度。状态必须有文字和图标，不能只靠颜色。

标题品牌字可少量采用本地「思源宋体 / Noto Serif CJK SC」；正文使用「思源黑体 / PingFang SC / Microsoft YaHei」；时间和引用编号使用系统等宽字体。未安装时使用明确 fallback，核心页面不依赖在线字体。

- 桌面使用约 220px 侧栏 + 内容区；证据详情使用约 400px 右侧抽屉，正文不长期挤成三列。
- 页面正文默认至少 16px，提供真实有效的大字模式；长说明书摘录保持舒适行高。
- 统一 4/8px 间距体系，轻量边框，克制阴影，主要容器圆角约 8–12px。
- 页面头部是当前患者记录摘要、待处理状态和主要操作，不做营销首页，不使用大面积渐变或无业务依据的大数字仪表盘。
- 代表性视觉元素是「记录 → 检查结果 → 证据」联动：点击某条风险后，在同一详情中看到相关药物、事实版本、原文和处理记录；每个节点必须有真实数据依据。
- 动效仅用于展开、状态变化、焦点定位，尊重 reduced motion，不播放虚构的 AI 思考过程。
- 移动端改为单列和底部主导航，次要导航进入「更多」。抽屉改为全屏详情，表格改为可读记录卡片。至少检查 390px、768px、1440px 视口。
- 弹窗支持 Escape、焦点约束和关闭后焦点返回；表单有 label；键盘可完成主要流程；异步状态有适度的 aria-live 提示。

主要路由：

| 路由 | 页面 | 核心作用 |
| --- | --- | --- |
| `/` | 照护总览 | 当前药单、有效预警、待核实事项、最近变化 |
| `/profile` | 患者档案 | 真实事实、来源、版本和核实操作 |
| `/medications` | 用药记录 | 新增、已发生的剂量变更、停用记录、版本历史 |
| `/alerts` | 风险与证据 | 预警、结论有效性、来源和复查 |
| `/conflicts` | 待核实事项 | 展示冲突双方并记录核实结果 |
| `/history` | 照护时间线 | 历史搜索、事件详情和双时间回溯 |
| `/assistant` | 照护助手 | 真实事件式查询与跨会话记录 |
| `/settings` | 设置与数据 | 服务状态、字号、真实导出与备份 |

引用和事件详情支持可分享于当前应用内部的深链接；不得自动公开生成患者信息链接。

### 五、逐页完整功能

#### 5.1 照护总览

首次进入并行读取真实状态和必要列表：

- 当前患者已记录的年龄、过敏、慢病、肝肾功能和用药数量。
- 当前有效预警、待复查结论、未决冲突。
- 最新照护变化，按真实时间排序。
- 尚未完成的提交任务及继续查看入口。

首要操作是「记录用药变化」和「补充患者情况」。第一次打开空数据库时，显示简短说明和上述入口，不自动创建患者或写入示例数据。

区分「还未记录」「没有查询到数据」「当前请求失败」「没有当前有效预警」。读取失败时不把计数置为 0，旧缓存可保留并标明更新失败和最后成功获取时间。

所有数量必须有明确口径；全量统计通过后端聚合取得，不能把第一页或截断列表长度当成总数。不展示没有数据基础的依从率、健康分、服药打卡率、预测风险分或血压曲线。

#### 5.2 患者档案

支持现有结构中的年龄、性别、体重、肾功能、肝功能、过敏史、慢性病和照护偏好。不要默认添加当前 schema 不支持的身份证、电话、住址等必填项。

字段与后端 payload 的对应：

| 表单内容 | 写入字段 |
| --- | --- |
| 年龄 | `payload.profile.age` |
| 性别 | `payload.profile.sex` |
| 体重 kg | `payload.profile.weight_kg` |
| 肾功能 | `payload.profile.renal_function` |
| 肝功能 | `payload.profile.hepatic_function` |
| 过敏列表 | `payload.profile.allergies` |
| 慢病列表 | `payload.profile.chronic_diseases` |
| 照护说明 | `payload.profile.preferences.care_notes` |

行为要求：

- 注册用 `register_profile`，补充更新用 `profile_update`，都经真实事件链写入。
- 修改时只提交用户意图新增或变更的字段，不重复提交整份未变档案以人为增加事实确认次数。
- 缺失的数字用空值，不默认填 0；肝肾功能不默认选「正常」。
- 展示事实来源、有效时间、记录时间、版本、`status` 和 `verification_status`。
- `recorded_as_reported` 表示「按报告记录」；`verified` 表示「记录已核实」，附核实来源或规则，不能统一写成「医生已确认」；`disputed` 表示存在待核实信息。
- 同一个关键事实存在多个争议版本时，完整展示相关版本，不取数组最后一条作为真相。
- 已有事实的删除意图走明确「撤回该记录」动作，保留理由和历史。过敏、慢病输入框移除标签不能静默删除事实。
- 对事实提供「查看历史与来源」「记录核实」「撤回记录」。核实与撤回对应后文必须补齐的真实适配接口。
- `verify_semantic_fact` 可能返回 `blocked_by_conflict`：此时展示待核实冲突，不能显示核实成功。
- 生效时间不明的 `uncertainties` 单独展示。当前 profile 事件没有完整传递生效时间的契约时，不得做一个实际无效的时间输入框；如支持用户更正历史生效时间，必须打通事件、存储与回溯并验证。

#### 5.3 用药记录

当前药单展示药名、后端归一化成分、剂量、途径、频次、开始时间、来源、版本及有关预警。没有记录的剂量或频次显示「未记录」，不填推荐值。

支持三种真实记录动作：

1. 新增一条已报告用药，`action: "add"`。
2. 记录已发生的剂量/途径/频次变化，`action: "dose_change"`。
3. 记录已经发生的停用，`action: "remove"`。

表单字段为药名、剂量、给药途径、频次、实际发生时间以及必要的报告说明。后端未支持独立结构化说明字段时，说明保留在事件 `text` 中；不要伪造已持久化字段。

- 药名可输入商品名或通用名，识别交给后端。若做搜索建议，必须调用基于真实药名映射/语料的接口；没有该接口时使用正常自由输入，不硬编码热门药名。
- 对已有药物的变更，从实时药单选择并携带后端当前识别所需的 `display_name`，不要让用户重新猜名称。
- `dose_change` 表单回填旧剂量、途径和频次，提交预期完整新值，避免未改字段因遗漏被后端置空。
- 提交前展示本次将记录的变化；操作文案使用「记录剂量变更」「记录停用」，表达用户报告的既有事实。
- 对同名重复新增说明后端可能去重或视为变更，以真实操作结果为准。
- 成功后刷新药单、档案上下文、时间线、预警、冲突和复查状态。
- 查看已停用、被替代记录及版本链，需要完整历史接口；不得把当前药单过滤一下当作完整用药史。
- 药单数量超过服务端分页上限时仍能完整浏览。
- `unresolved` 表示没有匹配到当前在用药，不能显示「已成功停用」。如当前事件结果没有结构化业务 outcome，按后文补齐，不解析自然语言来猜成功。
- 不增加自动剂量计算、系统建议停用或推荐新药按钮。

#### 5.4 风险与证据

同一个预警组件复用于事件结果、总览和预警中心。详情至少包括：

- 相关药物或相关患者条件。
- 后端原始严重度与中文标签。
- 后端原始置信度，不把 high/medium/low 换算成虚构百分比。
- 风险描述、证据类型、来源 URI 和原文摘录。
- 检查发生时间、记录时间、关联药单/事实引用。
- 结论状态、失效原因、替代结论以及复查状态。

已知严重度值为 `contraindicated / major / moderate / minor / unknown`；中文展示分别表达禁忌级提示、严重、需关注、轻微、未知，全部保持为风险信息，不转写成治疗命令。未知新枚举显示原值与「未识别状态」，不自动归为低风险。

预警中心按「当前有效」「待复查」「历史」组织。判断必须基于真实 conclusion 状态、`superseded_by` 和复查结果，不能根据用药是否在当前列表里自行推断结论有效性。

当前 `GET /v1/alerts` 只有结论文字和引用，不具备上述完整字段，而且没有过滤失效结论。必须按后文补齐历史预警读模型，不能直接将它映射成完整风险卡。

证据展示要求：

- 说明书原文、KEGG 佐证、本地检测器溯源分别呈现。存在一个本地文件指针不等于已有中文说明书原文。
- 展示服务端实际保存的 quote；只有拿到确切匹配的 chunk 才高亮相应位置，不从别处找相似文字填充。
- HTTP(S) 来源可打开；`memory:`、本地文件路径和语料内部 URI 通过应用内详情解析，不构造成 `file://` 或任意文件读取请求。
- 原文或来源缺失时明确说明缺失。不能为了美观隐藏低置信度、未知或没有中文原文的风险。
- `source_type` 含糊时保留原值，不包装成「权威临床结论」。
- 无预警显示「本次检查未形成带证据的新增警告」，不能显示「全部用药安全」。
- 高严重度、低置信度、不确定或冲突时，保留后端要求的医生/药师咨询提示。
- 结论失效后突出「待复查」，复查完成后显示新结论并保留原结论，不删除旧证据。

#### 5.5 待核实事项与医疗暴露记录

冲突列表展示主题、描述、创建时间、状态。详情并排展示 `left_ref` 与 `right_ref` 对应的真实记录、来源及版本；移动端上下排列。

冲突动作使用当前实际枚举：

| 用户操作 | `action` |
| --- | --- |
| 记录核实结果 | `resolved` |
| 标记本次无需继续处理 | `dismissed` |
| 重新打开 | `reopened` |
| 撤销上次处理 | `undo` |

提交保留 `basis`、`actor` 和有依据时的 `chosen_ref`。依据不能为空；`chosen_ref` 只能选择后端认可的真实引用，必要时补齐服务端校验。

动作完成后刷新冲突、相关事实、结论和复查任务。动作历史能够从后端恢复，关闭的冲突能够再次打开。当前 open-only 查询不足以支持这些体验，必须增加全状态查询与动作历史接口。

「记录核实结果」仅记录照护者的核实行为。当前 `resolve_conflict` 不等于自动把事实投影改成某一侧，页面必须分别展示冲突处理状态和事实状态。不能擅自替换事实或把「已处理」显示为系统已认定哪种医疗观点正确。

提供独立「记录造影剂暴露」表单，可从总览或时间线进入：

- 用 `procedure_exposure` 事件。
- 记录原始报告、实际发生时间、暴露物 `agent` 和 `doctor_involved`。
- `doctor_involved` 使用明确输入，不通过用户文本是否包含「医生」猜测，更不能默认 true。
- 当前支持的重点是含碘造影剂暴露，不能声称已经实现全种类手术/药械风险判定。
- 时间不明确时保存原始描述并如实标注；不要把「上个月」随意换算为具体一天。若接口把缺省时间当记录时间，展示该语义，不能称其为经核实的发生时间。
- 提交后展示真实返回的风险与医疗行为/说明书冲突两侧。

#### 5.6 照护时间线、搜索与历史回溯

时间线包含真实药物变化、情况报告、暴露事件、预警以及可用的关联处理记录。不同数据类型保留来源和含义，不把审计操作伪装为临床事件。

- 最新在前，服务端分页；按事件类型、药物/关键词和时间筛选。
- 同时展示「实际发生时间」与「系统记录时间」，必要时解释两者不同。
- 点击事件打开原始报告、结构化内容、引用、结论和审计动作。
- 搜索复用 `memory_search.search_history` 等真实数据能力；明确它返回历史候选，不把模糊搜索结果自动写成患者当前事实。
- 初期搜索只覆盖已落库的情景记录时，明确覆盖范围，不宣称搜索了全部患者档案和外部医学知识。

提供「查看历史状态」：

- `valid_at`：想查看的情况在什么时间有效。
- `known_at`：只使用截至什么时间系统已记录的信息。
- 用 `GET /v1/memory/state` 重新查询，不能在前端从当前快照回推。
- 顶部明确展示历史模式和两个时间，可一键回到当前。
- 历史视图默认只读，防止在旧状态上误操作。
- `uncertainties` 独立展示，不能隐去生效时间未知的报告。
- 用响应中 `open_conflicts` 的历史集合表示当时未决事项，不把返回行可能带有的当前处理状态冒充当时状态；需要展示历史状态字段时由后端归一化补齐。
- `meta.medications_revision` 等当前作用域修订号不能未经验证显示成该历史时点的修订号。
- 时间在 API 中保留 ISO 8601 与时区，界面按用户时区显示；默认可使用 Asia/Shanghai，不能把 UTC 无偏移地当成本地时间发送。

#### 5.7 照护助手与会话恢复

这是基于照护记录的事件式助手。所有发送走 `POST /v1/events`，显示真实完成响应。

- 普通输入用 `user_message`；明确当前药单查询可用 `query_current_medications`。
- 快捷问题只提供输入建议，点击后提交真实请求；不预埋回答。
- 展示返回的 `text`、`warnings`、`conflicts`、`audit_trail` 和 `safety_status`。
- 不隐藏后端安全拒绝或「结果可能不完整」提示，不在浏览器另外调用大模型改写答案。
- 原始正文可选安全 Markdown 渲染，禁用原始 HTML；引用通过严格的应用内 resolver 打开。
- HTTP 202 期间仅展示「已受理」「处理中」。没有流式协议时不做逐字流式，也不编造「已检索 128 份说明书」等进度。
- 同一会话有进行中的提交时，对相关写操作串行处理，避免重复发送和药单竞争；可以继续浏览其他只读页。
- 「开启新会话」更换会话标识，保留患者事实、药单和历史。不是清库。
- 刷新页面后可以读取真实历史会话及事件结果，恢复未完成任务，不只依赖内存里的聊天数组。
- 当前 HTTP API 未暴露完整会话历史，需要按后文增加服务端读取接口。从旧 Streamlit 记录导入显示时，没有保存的助手正文不要补造。
- 审计抽屉显示已落库的工具名称、执行状态、时间、引用与回退记录，不创造模型内部思考内容。
- 当前 API 返回值不包含 `tool_trace`。不能复制旧 UI 的空 trace 显示成「未执行工具」；应读取持久化的实际 trace。

#### 5.8 设置、服务状态与数据管理

提供真实有效的字号切换、时区显示偏好和界面偏好。

服务状态来自 `GET /v1/health`。服务不可达、后台 worker 未开启、有待处理任务等状态应分别说明。`status: "ok"` 只代表该端点的健康信息，不意味着外部模型或全部证据源均已验证可用。

- 显示后端实际启用的规划模式。`llm_planner_enabled` 不等于所有功能联网，也不等于本轮一定使用了模型。
- 不把 Streamlit 在进程内设置的默认环境变量误认为 FastAPI 已继承。实际数据源、语料版本和联网开关如需显示，应由服务端提供明确配置元数据；不能因磁盘上存在 v2 文件就标记当前使用 v2。
- 展示数据库 schema 版本、队列数量等可放在诊断折叠区，普通页面不暴露绝对数据库路径。
- 提供真实「导出照护数据」和「创建备份」及产物校验结果，复用 `backup.py`。导出文件名、时间、记录数、校验摘要来自服务端。
- 全库 JSON 交换导出是 `.json.gz`，不能把仅当前药单下载宣称为完整数据库导出。
- 浏览器可以单独打印当前真实药单/沟通摘要，使用专门打印样式，注明记录截至时间和来源；不要制造医生签名或临床证明。
- 恢复数据库涉及停写和目标路径管理，继续作为管理员维护能力；本轮不在普通照护页面增加覆盖当前数据库的按钮，不暴露任意文件路径输入。
- 不保留原演示 UI 的「清空并重新开始」作为正式产品常规操作。

当前仓库没有认证和多用户隔离。本轮不生成假登录、假权限设置或假家庭成员切换。交付说明明确受控单用户部署范围；若要新增公网多用户形态，需要单独实现后端认证、授权和患者隔离，不能靠前端路由守卫宣称完成。

### 六、必须遵循的当前 API 契约

下表描述的是当前代码已有能力。实施前重新读取代码或 OpenAPI，核实差异。

| 方法 | 路径 | 当前契约 |
| --- | --- | --- |
| POST | `/v1/events` | 必须提供 `Idempotency-Key`；异步受理，HTTP 202 |
| GET | `/v1/events/{idempotency_key}` | 查询 queued / processing / committed / failed |
| GET | `/v1/memory/state` | 可带 `valid_at`、`known_at`；返回 facts、medications、open_conflicts、uncertainties、meta |
| GET | `/v1/memory/timeline` | `limit` 默认 100，上限 500；当前按 occurred_at、id 升序取前 N 条 |
| GET | `/v1/memory/conflicts` | 只返回 open 冲突 |
| GET | `/v1/alerts` | 返回 `ref`、`memory_refs`、`source_refs`、`text`，不是完整 Warning DTO |
| POST | `/v1/conflicts/{conflict_id}/actions` | `{action,basis,actor,chosen_ref?}` |
| POST | `/v1/rechecks` | `{max_jobs}`；实际限制为 1–20，处理已有待复查任务 |
| GET | `/v1/health` | 状态、schema、队列数、规划开关、worker 配置、运行时间等 |

`GET /v1/memory/state` 的实际顶层结构：

```ts
interface MemoryStateResponse {
  valid_at: string;
  known_at: string;
  facts: SemanticFactDto[];
  medications: MedicationDto[];
  open_conflicts: StateConflictDto[];
  uncertainties: SemanticFactDto[];
  meta: {
    medications_revision: number;
    semantic_revision: number;
    future_leak_guard: boolean;
  };
}
```

具体行类型由 `memory.py` 的实际序列化字段定义，不要照抄 Python `snapshot()` 的 `semantic` 字段当作 HTTP 字段。`state.open_conflicts` 与 `/v1/memory/conflicts` 的当前行形状也有差别，需在 API 层明确适配。

事件提交结构：

```ts
interface EventRequest {
  event_type: string; // 当前 HTTP 是 string；客户端仅暴露已实现类型
  text: string;      // 长度 1–4000，必填
  payload: Record<string, unknown>;
  source: string;    // 通常 caregiver
  occurred_at: string | null;
  session_id: string;
}
```

在前端为已支持事件建立带判别字段的严格类型，内部组装以下 payload：

```ts
// register_profile / profile_update
{ profile: { age?, sex?, weight_kg?, renal_function?,
             hepatic_function?, allergies?, chronic_diseases?, preferences? } }

// medication_change
{ action: "add" | "remove" | "dose_change",
  medication: string, dose?: string, route?: string, schedule?: string }

// procedure_exposure
{ agent: string, doctor_involved: boolean }

// query_current_medications / user_message
{}
```

上述代码用于说明契约，不是合法 JSON 示例数据，更不是自动填充的患者内容。

提交成功受理体通常为：

```ts
{ event_key: string, status: "queued", status_url: string }
```

轮询：

- HTTP 202：`{event_key,status:"queued"|"processing"}`。
- HTTP 200：`{event_key,status:"committed",response:{text,warnings,conflicts,audit_trail,safety_status}}`。
- HTTP 500：`{event_key,status:"failed",error:string}`。

注意：重放已经 committed 的提交时，POST 仍可能返回 202，只携带 committed 受理状态，没有完整 `response`。此时仍需 GET 状态接口拿真实结果，不能把 POST 受理体当作最终回复。

普通错误结构：

```ts
{
  error: {
    code: string,
    category: "validation" | "safety" | "provider" | "internal",
    message: string,
    trace_id: string,
    details: Record<string, unknown>
  }
}
```

当前任务 failed 的轮询返回是上面另一种结构，不保证有 `trace_id`。客户端必须同时处理这两种格式、网络错误和非 JSON 响应，不能声称每个失败都有追踪编号。补齐服务端错误结构时保持兼容。

### 七、异步提交与幂等：必须完整实现

客户端流程必须覆盖：

```text
表单编辑
  → 校验并固定本次请求内容与幂等键
  → 提交中
  → queued / processing
  → committed：读取真实业务结果，刷新相关数据
  → failed：展示失败并提供受控重试

网络断开 / 轮询超时
  → 状态暂时未知
  → 恢复后查同一任务
  → 不擅自创建重复业务事件
```

具体要求：

- 每次新的用户提交生成一个满足 `[A-Za-z0-9_-]{1,128}` 的幂等键，例如 UUID；请求重发必须复用原 key 和完全相同的请求内容，包括 `session_id`、`occurred_at`、`source` 与 `text`。
- 同键同内容可能返回 `Idempotent-Replay: true`，视为同一次事件，不额外插入一条助手消息。
- 同键不同内容返回 422 `idempotency_key_reused`，说明需要使用新的提交操作，不能隐藏错误或自动拼接时间戳无脑重试。
- 已失败的 key 再提交返回 409 `previous_attempt_failed`。只有服务端明确失败且用户选择重试时，才创建新 key。
- HTTP 202 不是业务完成；HTTP 200 committed 也不意味着每个领域动作都成功，必须识别 unresolved、blocked 等真实 outcome。
- 默认轮询约 1 秒起步，上限约 5 秒，适度退避；后台标签页降低频率，恢复可见时查询。用 AbortController 清理无用请求。
- 处理时间可能超过 60 秒。超时后显示「仍在确认处理状态」，保留任务入口，不能宣告数据库未写入或自动重复提交。
- 页面刷新后至少能凭非敏感会话/请求标识恢复轮询，并从服务端找回请求元数据与结果。不要将整份健康记录常驻 localStorage 作为恢复方案。
- 丢失 POST 响应时先查询同 key 的状态；服务端未知该 key 时，保留同一请求尝试的语义。若刷新后连未确认请求正文也无法恢复，明确让用户重新确认内容，不猜测或生成新记录。
- 新会话不应遗失旧会话仍在处理的任务。任务列表独立关联原 session_id。
- 禁用重复点击和自动重试非幂等写请求。当前冲突动作、手动复查等接口没有事件接口相同的幂等保证，不能把事件重试策略直接套过去；需要可靠自动重试时先补后端去重。
- 未提供真实取消接口时，只允许停止等待/关闭详情，不能显示「已取消服务器任务」。
- 最终刷新使用查询 key 失效与重新拉取，处理旧请求覆盖新状态的竞争问题。

### 八、必须补齐的最小后端适配

以下是本次生成任务需要新增或完善的接口，不是当前已存在的接口。实际命名可以调整，但必须在 `docs/frontend/api-contract.md` 写清楚，并实现、测试、接入后再宣称功能完成。

不得删除或破坏旧端点兼容性；需要分页 envelope 或更完整 DTO 时增加独立读模型端点。所有数据从现有持久化记录和真实执行结果组装。

| 必需能力 | 建议新增接口 | 复用来源与约束 |
| --- | --- | --- |
| 完整预警列表/详情 | `GET /v1/alert-records`、`GET /v1/alert-records/{id}` | 关联 warning 情景、conclusions、真实引用、状态及替代链；复查生成的结论也需覆盖，不强行要求每条都有原始 warning episode |
| 分页事件历史 | `GET /v1/history/events` | 对真实 episodic 表查询，默认倒序，支持 cursor/limit/type/time；不能截取现有前 500 条后在浏览器假分页 |
| 全量药物版本 | `GET /v1/medication-records`、对应详情/版本查询 | 真实 medications 状态和 predecessor/version；包含 stopped/superseded 历史 |
| 全状态冲突与动作历史 | `GET /v1/conflict-records`、`GET /v1/conflicts/{id}/history` | 复用冲突表与 `conflict_actions_for`；保留 action 的真实序列 |
| 引用详情与审计 | `GET /v1/memory/item?ref=...` | `resolve_ref`、`audit_for`；检查引用类型和精确版本，不接受任意文件读取 |
| 结论版本链 | `GET /v1/conclusions/{id}/history` | `conclusion_chain`，完整解析当前替代关系，不伪造不存在的版本 |
| 历史候选搜索 | `GET /v1/history/search?q=...` | `memory_search.search_history`；返回真实 mode/results，说明范围与限制 |
| 事实核实与撤回 | `POST /v1/memory/fact-actions` | `{ref,action:"verify"|"retract",actor,basis}`，调用 `verify_semantic_fact` / `retract_semantic_fact`；映射真实 outcome |
| 复查任务详情 | `GET /v1/recheck-tasks` | pending 和历史 dependency_tasks、真实结论引用与状态；现有 POST 只执行已排队复查，不冒充全量重检入口 |
| 会话与提交记录 | `GET /v1/sessions`、`GET /v1/sessions/{id}/events` | 真实 interaction、outbox payload/result 及关联记录；保留每次提交的幂等键、状态和 session，不补造历史回复 |
| 轮次审计记录 | `GET /v1/sessions/{id}/turns/{turn_id}/trace` | `traces_for_turn`；只展示已有可审计执行记录，适度隐藏内部冗余和敏感细节 |
| 全量统计摘要 | `GET /v1/overview` | SQL/领域层真实聚合；明确 current/stale/open 的统计口径，不依赖分页结果条数 |
| 导出、备份和校验 | `/v1/data/exports`、`/v1/data/backups` 及受控下载/校验端点 | `export_database`、`backup_database`、`verify_artifact`；通过服务端产物 ID 下载，禁止用户指定任意路径 |

额外契约补齐要求：

1. 事件结果增加结构化 `operation_outcomes`（命名可等价），来自实际工具/领域返回，覆盖药物写入的 add/remove/dose_change/deduplicated/unresolved 等结果。不能仅返回统一「操作成功」；如发现原模板无条件声称已记录，做最小修复以与真实 outcome 一致，保留安全边界。
2. 预警读模型保留缺失字段为 null/unknown。不能从结论文本解析出所谓真实 severity；没有结构化证据的历史结论显示其真实文本、来源和缺失状态。
3. 备份/导出的内容要有一致快照语义；现有导出若跨表读取不能保证一致性，应先使用在线备份快照再导出或采用合适事务，不边写边拼接不一致文件。
4. 与 worker 共享写路径的新增操作必须使用正确锁/队列，`search_history` 的索引同步也可能写数据库，不能当作完全无副作用读操作忽略并发。
5. 所有新接口定义输入校验、业务失败返回、分页上限和参数允许值；对空字符串理由、非法状态、未知引用、版本不符返回明确错误。
6. 预警、冲突、药物列表以服务端过滤排序为准。cursor 使用稳定排序键，避免同一秒记录造成遗漏；不能硬编码一个「下一页」按钮重复返回相同数据。
7. 新增表或字段使用非破坏性迁移，并尽量复用已有结构。不要引入新的权威业务数据库或绕开 MemoryStore 另建一套健康记录。
8. 运行配置如需展示，增加只读 capability/config 元数据；前端不生成尚无写接口支持的「启用 LLM」「切换语料」假开关。

这些补齐项属于本次前端闭环所需的服务适配。不要借机重写 Agent 架构、重做检测模型或替换整个数据库。

### 九、全局交互和状态覆盖

每个页面与主要组件都实现：加载中、真实空状态、成功、局部失败、服务不可达、输入错误、处理中、重试中，以及适用的证据不全/历史状态。

- 骨架屏只表示布局，不摆放假患者名、药物或统计数。
- 保存失败保留用户填写内容，并把错误定位到字段或当前操作。
- 提交过程中修改表单不改变已固定的那次请求；下一次提交必须是新的明确操作。
- 不用全屏 toast 承载严重风险详情。重要结果留在可重新打开的记录或详情页。
- 证据、来源和用户文本按不可信内容渲染，禁止脚本执行和危险 URL 协议。
- 高风险或争议记录的核实、撤回、停用记录等动作提供简明操作确认，说明将记录什么变化。不要给普通只读查询增加确认步骤。
- 不因一次接口失败让整个应用白屏，相关页面提供局部重试；全局错误边界能恢复导航。
- 当前状态变化后的预警更新与复查完成能自动刷新，不能只有手动刷新整个网页才看得到。
- 不默认发送患者内容到分析、埋点或错误采集平台。日志诊断优先记录请求标识和错误类型。

### 十、实现顺序与交付要求

按以下顺序执行，但不要完成第一阶段就停止：

1. 核对代码和接口，记录契约与最小适配范围，确定页面结构和视觉 token。
2. 建立可启动的前端、应用壳、类型化 API 客户端和服务代理。
3. 优先打通档案登记、用药变更、异步轮询、真实落库与读取的主链路。
4. 完成预警读模型、来源/审计详情、冲突生命周期和复查展示。
5. 完成历史分页、历史回溯、会话恢复、搜索和数据导出/备份。
6. 完成移动端、大字模式、键盘操作、打印和所有异常状态。
7. 使用真实 FastAPI + 独立临时 SQLite 做联调与 E2E 验证，再检查页面视觉。

联调不得清空或写入用户现有 `stage0/memory.db`。用 `STAGE0_DB_PATH` 指向专门的临时数据库；表单输入测试数据明确为合成测试记录。正式应用启动不带 seed。

需要真实检测结果时调用实际 detector，使用仓库现有有来源的持久化证据。没有命中就报告实际结果，不能为了测试截图伪造预警。自动测试专用的可控故障或 provider 替身必须明确隔离，并与真实主链路验收分别报告。

最终交付包括：

- 完整前端代码、必需的服务适配、兼容迁移及测试。
- `.env.example`，只包含示例配置和变量说明，不包含密钥。
- `frontend/README.md`：开发启动、生产构建、后端启动、代理配置和单用户部署说明。
- `docs/frontend/api-contract.md`：区分原有与新增接口，记录 DTO、任务状态、错误与业务 outcome。
- `docs/frontend/acceptance-report.md`：验收场景、实际执行结果、截图位置、真实限制和未执行项。
- 桌面与移动主要页面截图；至少包含空状态、真实提交后的列表、预警或证据缺失状态、冲突详情、历史模式和错误状态。

启动文档至少给出并实际验证以下对应流程：

```powershell
# 仓库根目录：使用项目实际 Python 环境
python -m pip install -r stage0/requirements-stage1.txt
python -m pip install -r stage0/requirements-stage10.txt
python -m uvicorn stage0.server:app --host 127.0.0.1 --port 8000

# 另一个终端
cd frontend
npm install
npm run dev
```

如果选择其他包管理器，以真实项目脚本同步更新命令。构建至少通过类型检查和生产 build，新增的服务接口跑对应单元/集成测试，核心改动运行相关既有回归。

### 十一、验收标准：以下必须逐项验证

| 场景 | 必须观察到的真实行为 |
| --- | --- |
| 空数据库首次打开 | 没有虚构患者、风险、药单、对话或统计；提供可用的登记入口 |
| 登记患者情况 | POST 受理、任务完成、查询能读到；刷新和服务重启后仍存在 |
| 修改档案 | 只提交实际改动；版本与冲突遵循后端规则，未变字段不制造重复确认 |
| 新增用药 | 当前药单、时间线、检查结果来源于真实 API 和数据库 |
| 记录剂量变化 | 新旧版本可追溯；未修改的途径/频次没有意外丢失 |
| 记录停用 | 当前视图与历史视图变化正确；找不到在用记录时显示 unresolved |
| 连点、网络重发 | 同一事件只产生一次预期业务投影，不重复回复、不重复写记录 |
| 同键异请求 | 422 被正确解释，用户输入不丢失 |
| 刷新/断网后恢复 | 对同 key 继续查询；长任务不被误判失败或重新提交 |
| 任务真实失败 | 展示服务端失败，不用本地成功结果替代；新 key 重试是明确的新操作 |
| 预警与来源 | 原文、URI、memory ref 可核对；缺失证据被明确展示 |
| 结论失效和复查 | 旧结论不继续充当当前有效风险；复查后新旧结论可追溯 |
| 关键事实矛盾 | 冲突双方均可见，系统不静默选边 |
| 冲突核实/重开/撤销 | 真实动作落库；刷新后状态和动作历史一致 |
| 事实核实受阻 | blocked_by_conflict 不被显示成核实成功 |
| 历史回溯 | valid_at 与 known_at 独立；后补录/更正的信息不泄漏到较早知悉时点 |
| 超过 500 条历史 | 能查看新增的最新记录与完整分页，不受原有前 N 条查询误导 |
| 跨会话查询 | 新会话仍读取原事实和药单，历史回复从服务端恢复 |
| 只有部分 trace 或历史结果 | 显示实际可用记录，不补造缺失的执行步骤或助手回答 |
| 导出与备份 | 下载真实产物，数据数量和快照一致，校验结果来自服务端 |
| 服务停止 | 页面保留上下文并提示不可达，不显示假零值或伪成功 |
| 移动端与键盘 | 主要流程可操作，重要内容无横向溢出，焦点行为正确 |
| 构建与回归 | 类型检查/build/相关后端测试及关键 E2E 有真实执行记录 |

不要用「理论可用」「已预留」「按钮已添加」「后续接入 API」作为核心功能通过验收的证据。没有执行的检查标记未执行，不编造通过数量。

完成后用简明说明列出：实现了哪些闭环、增加了哪些后端适配、如何启动、哪些检查实际通过，以及仍存在的能力边界。目标是交付能够真实记录、真实查询、真实解释来源的照护应用。
