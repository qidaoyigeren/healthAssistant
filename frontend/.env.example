# 用药协管员前端 —— 环境变量示例(不含任何密钥)
#
# VITE_API_BASE:API 基地址(留空 = 同源,经 Vite 开发代理或生产反向代理转发 /v1)。
# 开发模式默认走 vite.config.ts 的代理:/v1 → http://127.0.0.1:8000。
# 仅当前端与 API 不同源且未配置反向代理时才需要设置(需服务端 CORS 明确允许该 origin)。
VITE_API_BASE=
