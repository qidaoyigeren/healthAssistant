# 用药协管员：记忆模块升级设计 Prompt

适用仓库：`D:\py\HealthAssistant`。调研与代码核对日期：2026-09-05。

使用方式：将下文「完整 Prompt」整段交给能读取本仓库的开发 Agent。它要求先交付设计、接口草案、迁移方案和验证计划；本轮不要求改造业务代码。末尾附有设计完成后可使用的实现指令。

## 设计建议与现状依据

建议把记忆模块的主线定为：**面向家庭用药协管的、带来源和双时间语义的可审计记忆；事实发生变化时，主动识别需要重新核查的历史结论。**

三个值得做深的技术点：

1. **受控事实写入**：识别是谁的情况、陈述是否包含否定或假设、凭什么记入档案；模型提出候选，程序决定写入行为。
2. **双时间记忆**：分别回答「按现在掌握的记录，那天发生了什么」和「在那天，系统当时知道什么」。
3. **结论依赖与失效传播**：事实更正后，通过依赖关系找到旧预警、摘要和待办，使其进入待重查状态，并保留原始依据。

上下文预算、摘要压缩、待核实事项和检索优化围绕这三个点服务。产品价值是减轻家属反复描述、整理药单和追溯变更的负担。

### 当前实现与具体缺口

| 代码位置 | 已有能力 | 本次核对发现 |
| --- | --- | --- |
| `stage0/memory.py:119` 起 | semantic / episodic / working、药单、conflicts、conclusions、audit_log | 已有版本、时间和来源字段；尚不能据此宣称完整事件溯源、双时间查询或防篡改审计 |
| `StructuredFactExtractor._deterministic_extract`，约 338 行 | 离线规则抽取 | 临时探针复现：「妈妈没有糖尿病」仍抽出 `present=true`；疾病匹配没有否定、假设和主体归属判断 |
| `_validate`、`write_semantic_fact`，约 309、449 行 | schema 过滤与按 namespace 处理冲突 | 允许显式 `conflict_policy=update`；临时探针复现：关键事实可被该参数更新，且不创建冲突。需要存储层强制策略，不能只依赖提示词 |
| `consolidate_interaction`，约 688 行 | 多种事实固化到数据库 | 临时探针复现：同一事件的第二条事实因非法 salience 失败时，第一条已保留；整体固化缺少明确的原子事务边界 |
| `retrieve_episodic`，约 774 行 | 按显著性与时间衰减排序 | `as_of` 仅充当衰减计算时间，未按该时间过滤事件；不能当成历史快照 API 使用 |
| `audit_for`，约 845 行 | 按 memory ref 找记录和审计日志 | 临时探针复现：请求 `@v999` 仍返回版本 1 的记录；旧行的状态也会更新，因此 ref 目前不是不可变历史快照保证 |
| `create_conflict`、`open_conflicts` | 保存冲突双方与开放冲突列表 | schema 预留 resolved/dismissed，但本文件尚无完整的冲突处理、撤销及依赖结论重查流程 |
| `record_conclusion`，约 815 行 | 保存 memory/source 引用 | 未看到结构化依赖索引、结论失效状态及更正后的重新检查闭环 |
| `stage0/agent.py` | CareEvent、MemoryRead/WriteTool、PlannerPolicyGuard、SafetyBoundary | 可复用现有工具与策略边界；无需为了记忆设计重写整个 Agent 框架 |

以上探针只使用合成输入和临时数据库，未读取真实患者记录或修改现有 `stage0/memory.db`。其余发现来自代码阅读。行号可能随后续修改变化，执行设计时须重新核对。这里没有声称运行过完整回归测试。

### 前沿方案的借鉴边界

以下是机制来源；如何用于本项目属于本方案的工程建议，不是这些框架已经替本项目实现的能力。

| 来源 | 可以借鉴的机制 | 本项目的取舍 |
| --- | --- | --- |
| [Mem0 论文，2025](https://arxiv.org/abs/2504.19413) | 从交互中提取、整合、检索有价值的信息 | 抽取结果先成为候选声明；关键事实的写入权限由确定性规则约束 |
| [Graphiti 官方仓库](https://github.com/getzep/graphiti) | 时间有效区间、双时间追踪、事实到原始事件的来源关系 | 先用 SQLite 表与关联表达时间和依赖；关键冲突由产品规则处理，不直接照搬自动失效策略 |
| [Letta Memory Blocks 官方文档](https://docs.letta.com/api/resources/agents) | 在上下文中保留有界记忆块 | 构建由程序生成的患者关键记录块；模型提出修改请求，不能任意重写事实底座 |
| [Mastra Observational Memory](https://mastra.ai/docs/memory/observational-memory) | Observer / Reflector、压缩历史、后台生成观察摘要 | 借鉴摘要整理流程；原始声明、关键事实和冲突保留在独立持久层 |
| [Mastra Memory Extractors，2026-07-21](https://mastra.ai/blog/introducing-memory-extractors) | 从观察流程提取结构化数据，向应用展示结果 | 让用户看见「记住了什么、待核实什么」；不必把 Python 项目迁移到其 TypeScript 技术栈 |
| [LongMemEval，ICLR 2025](https://arxiv.org/abs/2410.10813) | 信息抽取、跨会话推理、时间推理、知识更新、拒答评估 | 转化为家庭用药记忆的场景测试；公开分数不能当作本项目成绩 |
| [MemoryAgentBench，2026-06-28 修订版](https://arxiv.org/abs/2507.05257) | 增量多轮输入下评估记忆能力，包括准确检索与选择性遗忘 | 按交互顺序输入事件、持续提问，检查错误写入与更正是否影响后续行为 |

## 完整 Prompt

从下一段「你的任务」开始复制，直到「完整 Prompt 结束」。可连同上面的现状依据和来源表一起提供。

---

你的任务：担任熟悉 Agent 记忆、数据库时序建模和产品设计的资深工程师，基于当前 HealthAssistant 仓库，设计一套可落地、可演示、经得起技术面试连续追问的记忆模块升级方案。

我的目标是把这部分做成项目的核心技术贡献。设计必须解决家庭用药协管的实际问题；技术深度需要能通过数据模型、状态机、代码接口、失败案例和实验说明。请形成明确推荐，不要只罗列热门框架。

### 一、先读代码，明确范围

1. 阅读本仓库适用的 `AGENTS.md`、`README.md`、`DEMO.md`、`RESUME.md`、`stage0/memory.py`、`stage0/agent.py`、`stage0/app.py`、`stage0/test_stage3.py`、`stage0/test_stage5.py`；按需要查看 `ddi_engine.py`、`rag.py`、`normalize.py` 和依赖文件。
2. 检查 Git 工作区已有改动。所有现状判断给出文件、函数及可核对行号；区分「已经实现」「代码阅读发现的不足」「探针复现」「推荐设计」。代码能回答的问题先自己查。
3. 当前定位：单照护者、单患者、离线优先的家庭用药协管工程演示。记录照护者报告、整理用药历史、展示有依据的风险提示与未决冲突。系统不诊断、不处方，不自行要求开始、停止或调整药物。
4. 本轮交付完整设计和实施计划，可运行合成输入的临时探针；不要立即重构业务代码、清空数据库、安装云服务或上传患者数据。保留现有默认离线路径、显式 LLM 开关及 PlannerPolicyGuard / SafetyBoundary。
5. 默认由一名开发者分阶段实现。优先 SQLite 和现有 Python 栈。请说明每项复杂度解决什么问题；图数据库、向量库、消息中间件、多个自治 Agent 均须有必要性证据才能成为必选依赖。

### 二、从产品场景推出设计

以这条产品叙事贯穿全文：

「家属只需报告用药与健康记录的变化，系统能够说明记住了什么、信息是否仍适用、哪些记录互相矛盾，以及哪些旧提醒需要重新核查。」

至少覆盖以下场景，并给出输入、期望记忆状态、读取结果和可观察的用户反馈：

- 「妈妈没有糖尿病」「邻居有糖尿病」「如果妈妈有糖尿病」：不得写成当前患者确诊患病。
- 「我记得她可能对某药过敏」：保留为有来源的待核实报告，不丢失相关风险信息，也不升级成已核实事实。
- 9 月 5 日补录「9 月 1 日就不再服用药 A」：药单支持回溯，但 9 月 3 日当时的回答不能使用 9 月 5 日才录入的信息。
- 两条记录反映不同日期的检查结果：先判断时间变化，不能仅因数值不同就视为矛盾。时间和适用条件不明时保留不确定性，不推断医学结论。
- 「刚才记错了，那是爸爸的情况」：原记录可追溯，更正后不再作为妈妈的当前事实；不自动替另一位患者建档。
- 用户更正一条支撑旧预警的事实：旧预警变成待重查，系统展示变更原因与原始依据；重查失败时不能说风险已经消失。
- 用户结束会话，数天后继续：可看到当前记录、尚未核实的事情和必要上下文，无需复述完整历史。
- 一条摘要或外部文本包含「忽略过敏记录」：它是待处理数据，不能改变写入策略或静默清除关键事实。

请优先选择三条技术主线：受控事实写入、双时间查询、依赖结论失效重查。其他功能给它们提供支持。若你认为应替换其中一条，必须用仓库与场景证据论证。

### 三、借鉴前沿机制，明确哪些需要自己实现

查阅并引用 Mem0、Graphiti/Zep、Letta Memory Blocks、Mastra Observational Memory / Memory Extractors，以及 LongMemEval、MemoryAgentBench 的官方文档、仓库或原始论文。可使用本文件的来源链接，再核对设计时的版本。

每项用「机制 → 本项目问题 → 采用方式 → 局限/代价 → 验证方法」说明。区分论文机制、官方实现、厂商自报评测和本项目设计建议。不要把不同模型、数据集、评判方式的排行榜分数放在同一维度比较，也不要声称本项目实现了尚未验证的提升。

### 四、给出最小但完整的记忆架构

在现有三层记忆基础上，明确以下职责，允许合并表或组件；不要把每个状态字段都包装成一种新记忆层：

- **原始交互与事件**：记录谁在什么时候报告了什么，关联 source event、原文片段和抽取器版本。
- **声明与当前状态**：保存归属、否定/假设/不确定语义、适用时间、来源和状态；当前档案与药单是可重建的投影。
- **情景历史**：用药变更、报告的检查/暴露、核实动作、预警与交接，支持精确时间筛选与可选的模糊检索。
- **工作状态**：本轮任务和暂存结果，有明确会话边界与过期机制。
- **跨会话待办**：需要补充的记录、未解决冲突、待重查结论，持久化到解决或显式取消；不能随 working memory 过期而丢失。
- **派生内容**：预警、摘要、上下文块和检索索引；明确它们依赖哪些事实版本，失效后如何处理。

患者记录、药品说明书知识、工具观察和模型推断必须具有不同的来源类型。医生相关信息若只是家属转述，仍标为家属转述；不能因出现「医生」二字就获得更高的验证等级。

现阶段可以使用固定 patient_id，但读取、写入和依赖边界应明确主体。数据库字段预留不等于已经实现身份认证、多患者产品或租户隔离。

### 五、技术主线 A：受控事实写入

给出候选声明 schema，以及从候选到正式记忆的状态机。至少考虑：

- `subject_id`、`speaker/source_actor`、namespace、规范 key、原始值与规范值。
- `assertion_mode`：affirmed / negated / uncertain / hypothetical；引用他人或非目标主体单独表达。
- `source_event_id`、原文 span、来源类型、source URI（如有）、抽取器/模型/提示词版本。
- 抽取可靠性、来源验证状态、是否获得记录核实，分别表达；不要用一个任意 confidence 分数充当医学真实性概率。
- 对药名的归一化同时保留原名、药品/剂型标识、成分、剂量、途径等适用字段。成分重叠不代表两条用药记录可以合并；解析不出药名时进入 unresolved。

推荐管线：原始事件持久化 → 提取候选 → 结构校验 → 主体/语义/时间校验 → 去重与冲突分析 → 策略决策 → 原子提交状态变化 → 标记派生内容失效。

明确两个提交边界：原始输入可以先独立持久化并标记 pending；一次事件的事实、药单投影、冲突、审计与失效任务必须作为一个一致的提交单位。失败时不能留下部分生效档案；原始事件保留为 pending/failed，支持重试。

详细设计 MemoryPolicyGuard：

1. LLM 可以提出候选和解释，不得通过 `conflict_policy=update` 自行放宽关键事实策略；即使绕过 Agent 工具直接调用存储 API，也要校验。
2. `recorded_as_reported` 与 `verified` 有不同语义；低风险偏好、明确报告和关键事实更正采取不同处理方式，避免每条消息都强迫用户确认。
3. 明确否定不等于删除历史；新信息不总是比旧信息可靠；重复转述不等于多个独立来源。
4. 对无法可靠理解的否定、主体和相对时间，保留原文并提出有针对性的待核实项；离线降级不应把不理解的文字写成确定事实。
5. 为同一 input event 建立稳定幂等键；区分网络重试、重复事实、重复来源与同一文字描述的两次真实事件。不能仅依赖包含当前时间的内容哈希。
6. LLM 调用在数据库写锁之外完成；提交时重新检查预期版本并定义冲突重试。说明 SQLite WAL、进程内 RLock、数据库事务各自能保证什么。

至少给出候选 schema、策略伪代码、事务伪代码及三条规则通过/拒绝示例。

### 六、技术主线 B：双时间与冲突语义

设计 `valid_at` 与 `known_at` 两个独立查询参数：

- 有效时间：报告的情况何时发生或适用。
- 系统时间：系统从何时起掌握这一版本的信息。

给出时间字段、区间约定、索引、查询 SQL 和日期例子。字段命名可以选择 valid_from/valid_to 与 recorded_from/recorded_to，或用不可变事件重建系统时间；不要只加字段而没有查询语义。

必须区分：当前视图、用最新记录重建某天的情况、重现某天系统当时已知的情况。后录入的更正不得泄漏进更早的 known_at 查询。补录和乱序事件不得简单按插入顺序更新当前药单。

相对时间保留原文、时区和解析锚点；「上个月」可表示区间或未知精度，不能编造一个确切日期。年龄与体重需表达记录时点，不能无限期假定数值不变。

给出冲突分类与处理表：真实矛盾、正常时间变化、本人更正、来源不足、主体错误、适用范围不同。只有同一主体、同一规范事实、可比较语义与重叠适用范围的声明才进入对应的矛盾判断；不因语义相似就撤销记录。

设计冲突的 open → resolved/dismissed，以及必要时 reopened 的处理：谁进行了记录核实、依据是什么、作用范围与时间是什么、如何撤销处理。记录核实不代表系统进行了临床裁决。

设计稳定 ref 的解析契约：标识必须校验存在性、主体和版本；错误版本返回明确错误。不可变声明与会变的投影状态分开解释。普通 SQLite 审计日志只能称为可追踪，不能称为不可篡改证据。

### 七、技术主线 C：记忆更正后的结论失效与重查

给出可用 SQLite 实现的依赖模型，例如：事实版本 → 用药快照 → 检测观察 → 预警结论 → 摘要/待办。依赖必须来自真实执行时读取的记录，不能事后让 LLM 编造。

特别说明只记录「读到的几条事实」为什么不足：一次药单检查还依赖「当时完整的药单集合」，新增药物并不存在于旧引用中。因此设计 patient/medication snapshot revision、查询范围依赖或等价机制，处理新增事实与集合成员变化。

给出：

1. 事实更正或撤销如何查出受影响的结论、摘要和上下文缓存。
2. 即时标记 `stale/pending_recheck` 的事务边界，以及重新检测任务的持久化、去重与重试。
3. 旧结论的历史内容与依据如何保留，新结果如何形成新版本并关联前任。
4. 检测中途输入再次变化如何发现；结果提交前检查输入 revision，过期结果不得作为当前结论。
5. 重查失败/无证据/无匹配结果分别怎样展示；未检出不能直接解释为安全或风险解除。
6. 工具版本、规则版本和证据 corpus revision 如何进入依赖；没有接入外部更新信号时，不宣称能自动知道说明书已更新。
7. 删除源记录后如何阻止派生摘要或缓存把旧信息重新带回上下文。

推荐先用同库任务表/outbox 表达待重查；解释是否真的需要后台 worker。离线 MVP 可以在应用启动、相关事件或显式重查时消费任务；进程关闭期间不能声称持续运行或自动通知。

### 八、让记忆实际进入 Agent 的读取与决策

设计一个有界 ContextBuilder / ContextCompiler，并说明如何接入现有 memory_read、Planner、PolicyGuard 与最终回答，不要只设计一个没有消费者的新表。

建议分成：本轮相关关键事实与冲突、当前药单、任务相关历史、必要的待核实事项、低风险偏好。选取顺序必须可解释。

- 当前药单、关键事实、版本和时间范围走精确结构查询。
- 「上次提到白色药片是什么时候」等历史模糊问题，可增量试用 FTS/BM25 或向量检索；明确中文分词方案。向量相似度不决定当前事实真假。
- 相关的关键风险记录和未决冲突不能单靠时间衰减被挤出上下文。避免无关敏感信息全量常驻。
- 定义 token 预算、超限策略、included/excluded refs、snapshot revision、截断原因和事实新鲜度。关键集合装不下时显式标记不完整，通过工具分段读取或限制结论范围，不能静默遗漏后继续声称检查完整。
- “新鲜度不足”意味着需要更新信息；不等于旧事实自动为假。关键历史事实不随聊天遗忘策略删除。
- 摘要是可丢弃、可重建的派生缓存，必须携带来源范围与版本；更正后先失效再重建。避免只对旧摘要反复摘要造成否定、时间和冲突丢失。
- Observer / Reflector 如有必要，先实现为可控整理步骤；不能允许模型把自我反思固化为诊断或新的医疗规则。
- 缓存键覆盖主体、任务/查询、时间范围、输入版本和相关策略/证据版本。避免跨会话复用过期安全上下文。

请给出一次完整输入对应的 ContextPacket 示例，解释为什么包含或排除每一项。

### 九、用户界面与记录生命周期

在现有 Streamlit 里设计最少的交互：

- 本轮「已记录 / 待核实 / 存在冲突」。
- 当前档案、用药时间线和待核实列表。
- 查看一条结论的记录来源、更正链和重查状态。
- 更正误记、撤回记录、处理冲突；显示对当前档案及旧提醒的影响。

自然语言反馈与直接操作配合，不让用户面对数据库术语。低风险偏好可减少反复询问，但不得降低风险提示边界。

分别定义上下文淘汰、缓存清理、事实状态撤销和用户删除。关键历史保留与用户删除存在取舍，必须明确删除范围：原始文本、声明、摘要、索引、日志敏感字段和备份策略。不要声称逻辑删除等同于物理擦除，或承诺当前本地演示尚不具备的法定合规能力。

### 十、评测要能拆开定位错误

设计一个独立的记忆工程评测集，建议起步 40–60 条多轮场景，按家庭/患者故事模板分组划分开发集与留出集，防止只改人名日期造成数据泄漏。数量是建议规模，不是现有成果。

用合成数据和确定性 DDI/RAG fixtures 隔离记忆层；将真实检测器端到端评测单列。每一步记录期望声明状态、当前药单、冲突、可引用来源和应当拒绝的结论。输入按真实轮次递增，禁止一次性看见后续记录。

至少包括：否定/假设/第三人称；错主体更正；关键事实不可被模型直接覆盖；同义药名与复方歧义；同事件重试与两次同文事件；跨会话；晚到与乱序；双时间防未来信息泄漏；冲突解决与撤销；插入新药引起集合依赖失效；重查中再次更正；摘要丢否定；提示词注入；预算不足；模型不可用；事务故障；重启恢复；非法版本引用；用户删除后的派生记忆清理。

比较基线时控制差异：

1. 原始全历史/最近 N 轮作为回答阶段基线。
2. 当前 SQLite 三层记忆作为持久化与状态管理基线。
3. 新方案。

如果比较整条输入到输出链，使用相同原始事件；如果只比较读取阶段，使用相同已标注事实。明确各实验测量哪一段，不能用全历史聊天的回答分数直接代替数据库状态正确率。

要求以下指标的定义、分母、判定器与失败样例：

- 错误事实接受率，以及可正确提取事实的覆盖率，防止靠全部拒绝获得好成绩。
- 当前药单/事实状态准确率、双时间查询准确率、未来信息泄漏率。
- 冲突识别 precision/recall、解决状态正确率。
- 关键上下文覆盖率、旧结论失效覆盖率、无关结论误失效率。
- 来源与版本解析正确率、未知问题正确拒答率。
- 幂等重试、事务回滚和重启恢复通过率。
- 每轮 token、写入/读取 p50/p95、重查延迟、必要用户核实次数。

代码可判定的指标用确定性断言；语言回答评判可辅助使用 LLM judge，但必须记录模型与 rubric，并抽查。至少安排「移除写入策略」「只保留单时间」「移除依赖失效」「摘要替代原始事实」四项消融，说明验证的假设。

关键确定性反例作为发布前必须通过的回归门槛；有限样本通过不等于真实世界零风险。延迟、token 与准确率目标写为待测目标，禁止填写虚构结果。

### 十一、给出能实施的接口、迁移与阶段计划

输出 SQL 表结构草案、索引/唯一性/外键约束、核心 Python 类型与接口。下面只表达职责，可根据现有代码合并或改名：

`ingest_event`、`propose_assertions`、`validate_mutation`、`commit_event_projection`、`query_state(valid_at, known_at)`、`resolve_conflict`、`resolve_ref`、`invalidate_dependents`、`recheck_pending`、`build_context`。

至少给出四段接近可实现的伪代码：受控原子写入、双时间读取、依赖失效与结果版本校验、上下文构建。

迁移方案必须回答：如何从 schema_version=3 升级；如何保留旧 ID/ref；哪些来源片段与系统时间无法恢复；如何用 legacy baseline 表示历史边界；如何备份、验证、回滚；如何在临时数据库验证而不覆盖用户现有文件。不能从旧记录中编造缺失的时间或原文 span。

提供三个阶段，每阶段有明确收益、文件范围、验收用例和回滚方式：

- P0：修复错误记忆写入风险、存储层策略、引用版本校验、事务与事件幂等语义。
- P1：双时间与冲突闭环、依赖失效/重查、Agent 读取接入，完成主要演示和回归评测。
- P2：基于测量再增加历史混合检索、摘要整理、性能优化或多来源扩展。

每阶段估算一名开发者的人日范围与不确定性。另给出「只有 3 天」和「有 2 周」的裁剪方案。明确哪些设计先做接口预留，哪些必须真正实现才有资格写入简历。

### 十二、面试材料必须来自设计和证据

提供 90 秒项目讲法、三分钟演示脚本，以及 10 个递进追问。每个追问回答：为什么选它、替代方案、失败模式、如何证明、目前边界。

必须覆盖：

- 为什么关系型存储适合当前规模；向量检索或图结构何时值得引入？
- 为什么有 valid_from 和 created_at 还不等于实现了双时间？
- 这与普通数据库 CRUD 相比，新增的 Agent 问题在哪里？
- 模型提取错误如何不变成长期事实？JSON schema 为什么不够？
- 怎么区分「记录后来变化」与「以前就记错了」？
- 为什么更新事实后，只清缓存仍可能漏掉旧结论？
- 为什么仅引用旧药单中的药物 ID 会漏掉新增药物？
- 幂等、事务、乱序、重查并发与重启恢复怎样保证？
- 如何压缩上下文而不丢关键否定与冲突？
- 如何评价效果，避免漂亮的 demo、测试泄漏与虚假的医疗安全结论？

用三条可演示的技术贡献组织回答。简历表述区分「当前已实现」与「完成该阶段后才可使用」，所有收益数字引用本项目实测。不要预设我有团队规模、生产流量或临床效果。

### 最终交付格式

将方案写入中文 Markdown 设计文档，依次包含：现状审计；明确推荐与取舍；Mermaid 架构/时序图；schema 与状态机；四条关键流程伪代码；上下文与产品交互；迁移计划；评测与消融；分阶段实施；面试讲法和追问；机制来源链接。

每个主要模块都回答：解决哪个用户问题、何时触发、读写什么、失败怎么办、如何验收。遇到资料不足先说明可合理假设的范围；确实影响方向且代码无法回答的地方再集中提问。

请先开始阅读仓库并核对现状，再完成整份设计。不要把输出停留在概念清单或框架推荐。

---

完整 Prompt 结束。

## 设计完成后的实现指令

需要进入编码阶段时，可追加下面这段。这里是供之后使用的指令模板，本文件本身不表示已经完成实现。

```text
请依据刚完成的记忆设计，只实施 P0。先核对当前代码与设计的差异，保留工作区已有改动和现有离线路径。在临时数据库中完成有意义的错误输入、关键事实策略、事件幂等、事务回滚与版本引用测试，再运行适用的 Stage 3 / Stage 5 回归。产出变更说明、测试证据与仍存在的限制；不要清空现有 memory.db，不要把 P1/P2 一起展开，也不要填写未测量的收益数字。
```
